---
import Layout from '../../layouts/Layout.astro';
import ContactSection from '../../components/ContactSection.astro';
import { t } from '../../i18n/ui';

const lang = 'fr';
const T = t(lang);
---

<Layout title={T('prerolls.meta.title')} description={T('prerolls.meta.desc')} lang={lang} path="/fr/prerolls">
  <ContactSection
    lang={lang}
    title={T('prerolls.formTitle')}
    showLeadIn
    context={T('prerolls.ctx')}
    defaultSubject="sales"
    fullHeight
  />
</Layout>
