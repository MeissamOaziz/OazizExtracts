---
import Layout from '../../layouts/Layout.astro';
import ContactSection from '../../components/ContactSection.astro';
import { t } from '../../i18n/ui';

const lang = 'fr';
const T = t(lang);
---

<Layout title={T('flower.meta.title')} description={T('flower.meta.desc')} lang={lang} path="/fr/flower">
  <ContactSection
    lang={lang}
    title={T('flower.formTitle')}
    showLeadIn
    context={T('flower.ctx')}
    defaultSubject="sales"
    fullHeight
  />
</Layout>
