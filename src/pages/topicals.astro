---
import Layout from '../layouts/Layout.astro';
import ContactSection from '../components/ContactSection.astro';
import { t } from '../i18n/ui';

const lang = 'en';
const T = t(lang);
---

<Layout title={T('topicals.meta.title')} description={T('topicals.meta.desc')} lang={lang} path="/topicals">
  <ContactSection
    lang={lang}
    title={T('topicals.formTitle')}
    showLeadIn
    context={T('topicals.ctx')}
    defaultSubject="sales"
    fullHeight
  />
</Layout>
